/**
 * Created by emily on 2016/10/16.
 */
(function (w) {
    function bird(options) {
        this.ctx = options.ctx;
        this.img = options.img;
        this.x = options.x || 10;
        this.y = options.y || 10;
    }


    util.extend

    w.bird = bird;
}(window));